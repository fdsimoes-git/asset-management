{
  "name": "asset-management",
  "version": "1.3.0",
  "description": "Secure Asset Management System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:prod": "sudo node server.js",
    "ssl": "mkdir -p ssl && openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout ssl/server.key -out ssl/server.crt -subj '/C=US/ST=State/L=City/O=AssetManagement/CN=asset-manager.local'",
    "hash-password": "node -e \"const bcrypt = require('bcryptjs'); const pw = process.argv[1]; bcrypt.hash(pw, 10).then(hash => console.log('ADMIN_PASSWORD_HASH=' + hash));\" "
  },
  "dependencies": {
    "@capacitor/core": "^8.0.2",
    "@capacitor/ios": "^8.0.2",
    "@google/genai": "^1.42.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "express-rate-limit": "^8.2.1",
    "express-session": "^1.17.3",
    "helmet": "^7.1.0",
    "multer": "^2.0.2",
    "node-fetch": "^2.7.0",
    "nodemailer": "^8.0.1",
    "pdf-parse": "^1.1.1",
    "sdk-node-apis-efi": "^1.2.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.2"
  },
  "overrides": {
    "minimatch": ">=10.2.1"
  }
}
